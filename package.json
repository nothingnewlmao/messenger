{
  "name": "project-hw-1",
  "version": "1.0.0",
  "description": "messenger for project 1",
  "scripts": {
    "dev": "npm run watch && npm run start",
    "watch": "parcel watch ./src/*",
    "start": "rm -rf dist && npm run build && npm run express",
    "build": "parcel build src/pages/index.html src/pages/**/index.html --public-url ../",
    "express": "node app.js"
  },
  "keywords": [],
  "author": "Valentine",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.17.1",
    "parcel-bundler": "^1.12.5",
    "precss": "^4.0.0",
    "sass": "^1.35.2"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "parcel-plugin-asset-copier": "^1.1.0"
  },
  "assetsPath": "src/assets",
  "engines" : {
    "node" : ">=0.12"
  }
}
